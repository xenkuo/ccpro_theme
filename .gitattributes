# Set default behavior to automatically normalize line endings.
* text=auto

# Explicitly declare LF for all text files to ensure cross-platform consistency
text eol=lf

# JSON files should always use LF
*.json text eol=lf

# Windows scripts should use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
